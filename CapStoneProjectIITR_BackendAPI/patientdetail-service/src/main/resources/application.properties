spring.cloud.vault.host=localhost
spring.cloud.vault.port=8200
spring.cloud.vault.scheme=http
spring.cloud.vault.authentication=token
spring.cloud.vault.token=hvs.vmsufUuY8nNFNNdDS1xB184u
spring.cloud.vault.application-name=patientServiceVault5

server.port=0
spring.application.name=patientdetail-service
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
spring.config.import=vault://,configserver:http://localhost:8888/
management.endpoints.web.exposure.include=*